import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    primary: { main: '#7fd5e0' }, // Purple and green play nicely together.
    secondary: {
      main: '#406b70',
      light: '#bdbdbd'
    }
  }
});
